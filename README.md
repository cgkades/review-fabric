# Review Fabric

An evidence-driven, multi-agent code-review orchestrator.

The initial protocol is specified in [`openspec/changes/establish-review-protocol`](openspec/changes/establish-review-protocol/). The MVP is local and read-only: it does not change reviewed code, write GitHub comments, create commits, or merge pull requests.
